# flarecrawl — architecture flows

> **⚠ Stale (2026-06-13):** This map predates two structural changes —
> `cli.py` was split into a `cli/` package (17 command-family modules) and
> the MCP server (`mcp_serve.py` + `mcp_tools/`) was added. The component
> graph and `cli.py:line` citations below are out of date. Regenerate with
> codebase-cartographer for an accurate map.

Generated by codebase-cartographer · 2026-05-18 · 16 components · 11 flows.
Every hop cites a `file:line`. The interactive map is `index.html` (serve
the folder over HTTP). The source of truth is `flows.json`.

> flarecrawl is a single flat Python package (`src/flarecrawl/`, ~45
> modules, ~19.5k LOC). `cli.py` (5,960 LOC, Typer) is the orchestrator;
> the modules below are its collaborators. `config` is imported by 26
> modules — the de-facto backbone.

Legend: `→` import/call · *(partial)* = spine traced, some hops inferred (see notes).

---

## scrape — Scrape a single URL to markdown  ✅
`flarecrawl scrape <url>`

```
cli ─→ client       construct HTTP API Client          cli.py:425
cli ─→ extract      main-content extraction → markdown  cli.py:393
cli ─→ sanitise     sanitise_html()                     cli.py:399
cli ─→ blockdetect  detect bot-challenge / block        cli.py:1251
```

## scrape-browser — Scrape via headless browser (CDP)  ✅
`flarecrawl scrape <url> --render` · `screenshot` · `pdf`

```
cli ─→ cdp     CDPClient — Chrome DevTools Protocol  cli.py:436
cdp ─→ config  account_id / api_token                cdp.py:16
```

## scrape-paywall — Paywall / anti-bot bypass  ⚠ partial
scrape hits a paywalled or DataDome-protected page

```
cli     ─→ paywall  _try_stealth_fetch()                cli.py:1413
paywall ─→ stealth   curl_cffi browser-TLS fingerprint  paywall.py:286
```
*curl_cffi import is lazy inside `_try_stealth_fetch`; mechanism evidenced by paywall.py:9 docstring.*

## crawl — Crawl a site  ✅
`flarecrawl crawl <url>`

```
cli    ─→ client  client.crawl_status() / crawl_get()  cli.py:3049
client ─→ config  Cloudflare account config            client.py:10
```
*CF /crawl always respects robots.txt (cli.py:3122).*

## search — Web search  ✅
`flarecrawl search <query>`

```
cli ─→ search  jina_search()  cli.py:2622
```

## batch — Batch process a URL list  ⚠ partial
`flarecrawl batch <file>`

```
cli   ─→ batch   parse_batch_file / process_batch  cli.py:34
batch ─→ client  process_batch fans out scrapes     batch.py:49
```
*client is closured into `process_batch` (async), not imported in batch.py.*

## map — Map a site's URLs  ⚠ partial
`flarecrawl map <url>`

```
cli ─→ client  Cloudflare /map endpoint                  cli.py:3242
cli ─→ robots  respects robots.txt / AI Crawl Control    cli.py:3122
```

## discover — Discover feeds / sitemaps / links  ⚠ partial
`flarecrawl discover <url> --sitemap`

```
cli ─→ sitemap  XML sitemap discovery  cli.py:4216
```

## auth-login — Authenticate to Cloudflare  ⚠ partial
`flarecrawl auth login`

```
cli ─→ client       validate credentials with a token Client  cli.py:602
cli ─→ credentials  store / validate credentials              cli.py:600
```

## design-extract — Extract a site's design system  ✅
`flarecrawl design <url>`

```
cli ─→ design  EXTRACT_JS, process_tokens, score_coherence  cli.py:5337
```

## config-backbone — Shared config backbone  ✅
every command resolves Cloudflare account/token + proxy

```
client ─→ config  account, token, proxy   client.py:10
cdp    ─→ config  account_id / api_token   cdp.py:16
```
*`config` is the most-imported module in the package (26 importers).*

---

_Validated: schema valid, all 21 cited lines re-checked against source — no drift._
